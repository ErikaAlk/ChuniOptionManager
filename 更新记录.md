# 更新记录

> 倒序，最新在上。

## 2026-08-28 — v1.1.0 窗口背景换成 Mica

主窗口和「选 option 文件夹」那个对话框的背景改成 Windows 11 的 **Mica**：DWM 拿桌面
壁纸做一层模糊去饱和的底，跟着壁纸和亮/暗模式走。卡片、分组框、检查器仍然是不透明
的实心底——内容压在实心底上才读得清，Mica 是给窗口底的。

要三件事一起才有效果，少一件就只是个透明窗口：窗口自己不画底
（`WA_TranslucentBackground` + 样式表里两条 `[mica="true"]`）、
`DwmExtendFrameIntoClientArea` 把玻璃摊到整个客户区、属性号按版本分
（22H2 起用 `DWMWA_SYSTEMBACKDROP_TYPE`，21H2 只认没进文档的 `DWMWA_MICA_EFFECT`）。

挂不上就退回原来的 `#1C1C1E`：Windows 10、以及 DWM 返回错误的机器一个字节都不受影响。
**半路失败会把透明属性收回去**——透明而底下没有材质，看到的是一个黑窟窿，比没有
材质难看得多。

弹出式的对话框（新增角色、单图快速生成、作品库）保持不透明：Mica 取的是桌面壁纸而
不是身后那扇窗，盖在主窗口上的临时窗口用它只会让人对不上位置。

测试 102 → 107 条，五条新的都拿变异测试验过（把修复逐条改回去，确认对应的那条真的红）。

## 2026-08-27 — v1.0.0 整个重写成 Python，并搬出游戏目录

### 换了技术栈

WinUI 3 / .NET 8 的那一版整个删掉，改成 **Python 3.11 + PySide6**。原来 4688 行
C# / XAML，现在 4000 行左右的 Python，加上 102 条测试。旧代码在 git 历史里，
标签 `winui-final`。

换的理由是它一直缺一条路：WinUI 那版没有安装包，只能靠「exe 埋在
`bin\x64\Release\net8.0-windows10.0.19041.0\` 底下，再往根目录放个快捷方式」凑合，
换台机器就得重新配一遍 .NET 和 Windows App SDK。现在
`packaging\build.py` 一条命令出 42 MB 的安装包，目标机器什么都不用装。

### 不再住在游戏目录里

以前这个仓库就摆在 `option\ChuniOptionManager\`，靠「我在哪」反推 option 根目录——
于是它自己在「写入包」的下拉框里也是一个可选项，而且游戏目录被清理或整包替换时会
跟着遭殃。

现在程序装到 `%LOCALAPPDATA%\Programs\ChuniOptionManager`（不弹 UAC），
**option 目录在安装时问**：安装向导多一页，自动探测常见路径，选游戏根目录或 `bin`
也能认出来，当场校验「里面有没有 A001/A300/AXVX 和 Music.xml」。结果写进
`%APPDATA%\ChuniOptionManager\option-root.txt`；应用自己的设置在同目录的
`config.json` 里，**安装器从不碰它**——升级安装不该把人在应用里换过的目录顶回去。

跳过那一页也行，第一次打开时会再问一次。应用里也能随时「切换 option 目录」。

### 顺手修好的

- **曲绘现在真的显示了。** option 里 730 张曲绘全是 `.dds`，老版本直接把 `.dds` 喂给
  WinUI 的 `BitmapImage`，于是每一张卡都写着 NO IMAGE。那不是没有图，是根本没解码。
- **保存谱面开关不再重排整份文件的缩进。** 改成字节级改写，除了 `<enable>` 之外一个
  字节都不动。老版本靠 `PreserveWhitespace` 尽量少动，但仍然会重写整份文件。
- **`xmlns:xsd` / `xmlns:xsi` 不会再丢。** 这两行在 `DDSImage.xml` / `CharaWorks.xml` /
  `WorksSort.xml` 的根节点上挂着但一次都没用到，ElementTree 默认只写它实际用到的
  命名空间——读进来再写出去会静默消失。现在每次写回都原样补上，
  **读进来再写出去是字节级一致的**。
- **写出去的 XML 结尾不再多一个换行**，和游戏自己发出来的文件收尾方式一致。
- **扫描跳过点开头的目录**（`.git` / `.vs` / `.venv`）。option 根目录里塞过工具目录的
  人会用上这一条。
- 「单图快速生成」里那根「覆盖图透明度」滑杆**现在有东西可以透出来了**：底下垫的是
  模板贴图，用来对位。老版本底下什么都没有，滑杆只是把图调暗。
- `requirements.txt` 改成纯 ASCII。**没有 BOM 时 pip 按本机 locale 编码去解**这个文件，
  中文 Windows 上一个中文注释就让 `pip install -r requirements.txt` 在装第一个包之前
  报 `'gbk' codec can't decode byte`——在新克隆的仓库上实测踩了一次。

### 界面

照 Apple HIG 重做：系统语义色、macOS 字体样式表、inset grouped 版式、8pt 网格、
自绘的拨动开关。主题色仍然是紫罗兰 `#B44BFF`，但现在写死的只有 `ACCENT` 一行，
hover / pressed / 半透明态全从它推导。

编辑面板从「压着半透明黑底的浮层」改成右侧常驻的检查器——浮层会把整个列表压暗，
而编辑的时候恰恰要能同时看见列表。

歌曲行、角色格、排查行改成自绘（`QStyledItemDelegate`）。600 多首歌各建一棵控件树
会明显发涩，自绘之后只有可见的那十几行在画。

### 测试

102 条，`pytest`，不需要显示器也不碰真实的游戏目录（在临时目录里造一棵假的 option 树）。
按「测试必须能失败」验过一遍：**把修复改回去的变异逐条验过，全部被对应的测试挡住**，
包括「命名空间声明不补回去」「保存开关退回整份重写」「删除时不检查是否在 option 根内」
「BC3 端点取头两个像素而不是最远的一对」这些。

打包脚本四步：测试 → PyInstaller → **启动冒烟** → Inno Setup。冒烟那步不能省，
少一个隐藏依赖的表现就是双击没反应，而在源码里跑是看不出来的。

### 核对过的

- 102 条测试全绿；变异测试逐条验过，把修复改回去都会红在正确的那条上。
- 打包链路整条跑通：exe 151 MB、安装包 42.3 MB，启动冒烟拉起真窗口。
- 1080 的 `big.dds` 编出来正好 1,556,896 字节，和游戏自带的那份一模一样。
- 安装器的「选择 option 文件夹」那一页真开出来看过：中文没乱码，
  自动探测填出了 `C:\Chuni\CHUNITHM\bin\option`。
- 拿真实 option 目录跑过：611 首歌、68 个角色、7 条排查项，曲绘和立绘都出图。

## 2026-08-25 — 去掉界面和 README 里那股 AI 腔

主窗口标题下面那行「可视化查看和管理 option 文件夹里的歌曲、谱面和角色」删了。
名字就是名字，打开这个应用的人不需要被告知它是干什么的，而右边「Option 根目录」
那一行本来就写着当前扫的是哪个目录。

README 的  标题前面挂的 emoji 全部去掉；功能那一节原来是四组加粗伪标题
（歌曲 / 谱面、角色、作品库、排查、通用），改成正常的  三级标题。
加粗从 15 处减到 2 处，只留「删除作品会连带删除属于它的角色」和
「必须带 -p:Platform=x64」——这两条踩了是有代价的。

Release x64 构建过，0 警告 0 错误。

## 2026-08-23 — 主题色换成紫罗兰

强调色以前跟着 **Windows 个性化设置**走，几个自己写的 App 摆在一起长得一模一样。
现在每个项目认领一种主题色（表在全局 `~/.claude/CLAUDE.md`），
CHUNITHM Option Manager 拿到的是**紫罗兰 `#B44BFF`**。

`App.xaml` 里覆盖 `SystemAccentColor` 及 Light1/2/3、Dark1/2/3 六档
（主色混白 / 混黑 14%、28%、42% 推出来的），外加 `AccentFillColorDefaultBrush`
三档。按钮上的字维持白色：白字对这支紫 3.90:1，黑字 5.39:1，差不多，白的更像平台惯例。

顺手删掉 `AccentGreen`（`#FF00A985`）——全仓没有任何地方引用它，
留着只会让人以为主题色是绿的。
